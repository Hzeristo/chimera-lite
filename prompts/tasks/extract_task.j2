Extract the mechanism-level Knowledge claims from the paper below, as JSON matching this schema:

{{ json_schema }}

Return 1-5 claims — mechanism `statement`s (no numbers), each with a `falsification`, grounded
`sources` (verbatim quotes), a `status`, `tags`, and any functional-defect `flags`. Leave
`proposed_edges` empty (edges are added by a separate grounding step).

--- PAPER: {{ paper.title }} ---

{{ paper.raw_text }}

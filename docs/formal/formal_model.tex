% Chimera Lite — Formal Model
%
% Reformat only: the body below is the arrived formal model. No mathematical or
% prose CONTENT was added, removed, or reworded — every symbol, identifier, and
% sentence is as received. The changes are markup repairs only:
%
%   1. This preamble + \begin{document} / \end{document}.
%   2. Two math blocks written as inline $...$ contained alignment markers (& and
%      \\), which inline math cannot carry — the source as received did not
%      compile. They are wrapped in align* instead; every token inside is
%      unchanged. Affected: "Evidence Tiers" and "Tag semantics".
%   3. Set literals were written with bare braces, e.g. {\mathtt{K}, \mathtt{T}}.
%      In LaTeX bare braces GROUP silently rather than print, so every set in the
%      document rendered without its delimiters. Repaired to \{...\} at 19 sites.
%      This restores the intended notation; it introduces no new symbols.
%
% Build:  pdflatex formal_model.tex

\documentclass[11pt]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{stmaryrd}   % \llbracket, \rrbracket
\usepackage{geometry}
\geometry{margin=1in}

\begin{document}

\section{Formal Model}

\subsection{Research Artifact Graph}

Let $\mathcal{G} = (V, E, \tau, \sigma, \lambda, \preceq)$ where:
\begin{itemize}
\item $V$ is a finite set of \emph{artifact nodes}
\item $E \subseteq V \times V$ is a set of \emph{typed edges}
\item $\tau: V \to \{\mathtt{K}, \mathtt{T}, \mathtt{I}, \mathtt{D}, \mathtt{W1}, \mathtt{W2}\}$
      is the \emph{node type function}
\item $\sigma: V \to \{\mathtt{candidate}, \mathtt{staged}, \mathtt{committed}, \mathtt{stale}\}$
      is the \emph{lifecycle status}
\item $\lambda: E \to \{\mathtt{derives\_from}, \mathtt{depends\_on}, \mathtt{collides\_with},
      \mathtt{contradicts}, \mathtt{supersedes}\}$ is the \emph{edge label function}
\item $\preceq \subseteq V \times V$ is a partial order on nodes induced by provenance
\end{itemize}

\subsection{Evidence Tiers}

Evidence sources are stratified into three tiers:
\begin{align*}
\text{Tier}_1 &= \{\text{table entries, equation definitions, arXiv metadata}\} \\
\text{Tier}_2 &= \{\text{method descriptions, result statements with context}\} \\
\text{Tier}_3 &= \{\text{author claims in intro/conclusion}\}
\end{align*}
with epistemic warrant ordering $\text{Tier}_1 \succ \text{Tier}_2 \succ \text{Tier}_3$.

\subsection{Verification Tags}

Let $\mathcal{T} = \{\mathtt{V}, \mathtt{P}, \mathtt{U}\}$ with numeric encoding
$\llbracket \mathtt{V} \rrbracket = 2$, $\llbracket \mathtt{P} \rrbracket = 1$,
$\llbracket \mathtt{U} \rrbracket = 0$.

A verification judgment is a triple $\langle c, s, q \rangle$ where:
\begin{itemize}
\item $c$ is a claim (syntactic term)
\item $s \in \mathcal{T}$ is the verification tag
\item $q$ is a verbatim quote from source $\in \text{Tier}_1 \cup \text{Tier}_2$
      (empty if $s = \mathtt{U}$)
\end{itemize}

\textbf{Tag semantics:}
\begin{align*}
\mathtt{V}(c) &\triangleq \exists q \in \text{Tier}_{1,2}.\ q \vdash c
  \land \text{cited}(q, c) \\
\mathtt{P}(c) &\triangleq \exists q.\ q \rightharpoonup c
  \land (\text{confounded}(q) \lor q \in \text{Tier}_3) \\
\mathtt{U}(c) &\triangleq \neg \exists q \in \text{checked sources}.\
  q \vdash c \lor q \rightharpoonup c
\end{align*}
where $q \vdash c$ denotes entailment and $q \rightharpoonup c$ denotes partial support.

\subsection{State Transitions}

The artifact graph evolves under two clocks:
\begin{itemize}
\item \textbf{Machine-time} $t_m \in \mathbb{N}$: candidate accumulation
\item \textbf{Human-time} $t_h \in \mathbb{N}$: truth commitment ($t_h \ll t_m$ in cardinality)
\end{itemize}

\textbf{Candidate production} (machine-time transition):
\begin{equation}
\frac{\text{W1}(c, p) = \langle c, s, q \rangle}
     {\mathcal{G} \xrightarrow{t_m} \mathcal{G}' \text{ where }
      V' = V \cup \{v_{\text{new}}\}, \sigma(v_{\text{new}}) = \mathtt{candidate}}
\end{equation}

\textbf{Human commit} (human-time transition):
\begin{equation}
\frac{v \in V, \sigma(v) = \mathtt{candidate}, \text{Architect}(v) = \texttt{promote}}
     {\mathcal{G} \xrightarrow{t_h} \mathcal{G}' \text{ where }
      \sigma'(v) = \mathtt{committed}}
\end{equation}

\textbf{Provenance monotonicity} (structural gate):
\begin{equation}
\forall v \in V.\ \sigma(v) = \mathtt{committed} \implies
  \llbracket \text{tag}(v) \rrbracket \leq
  \min_{u \in \text{deps}(v)} \llbracket \text{tag}(u) \rrbracket
\end{equation}
where $\text{deps}(v) = \{u \mid (u,v) \in E, \lambda(u,v) = \mathtt{depends\_on}\}$.

\textbf{Provenance decay} (temporal propagation):
\begin{equation}
\frac{(u,v) \in E, \lambda(u,v) = \mathtt{derives\_from},
      \sigma(u) \xrightarrow{t} \mathtt{stale}}
     {\sigma(v) \xrightarrow{t} \mathtt{stale}}
\end{equation}

\subsection{System Invariants}

\textbf{I1. Human-time supremacy:}
\begin{equation}
\forall v \in V.\ \sigma(v) = \mathtt{committed} \implies
  \exists t_h.\ \text{action}_{\text{human}}(v, t_h)
\end{equation}

\textbf{I2. Tier-grounded verification:}
\begin{equation}
\forall v.\ \text{tag}(v) = \mathtt{V} \implies
  \exists q \in \text{Tier}_{1,2}.\ \text{anchor}(v, q)
\end{equation}

\textbf{I3. Well-formed support chain:}
\begin{equation}
\forall v \in V_{\text{committed}}.\
  \text{reachable}(v, \text{Tier}_1 \cup \{\text{human-authored}\}) \lor
  \text{tag}(v) \in \{\mathtt{P}, \mathtt{U}\}
\end{equation}
A committed node either traces to Tier-1 evidence or explicitly admits weakness.

\textbf{I4. Append-only with supersession:}
\begin{equation}
\forall v, v' \in V, t < t'.\ \sigma(v, t) = \mathtt{committed} \implies
  \sigma(v, t') \in \{\mathtt{committed}, \mathtt{stale}\}
\end{equation}
Committed nodes are never deleted, only superseded and marked stale.

\subsection{Phase Locus Colligo: Reduction Operations}

\textbf{R1. Claim extraction} (K-node $\Rightarrow$ W1-queue):
\begin{equation}
\frac{\tau(v) = \mathtt{K}, \text{claims}(v) = \{c_1, \ldots, c_n\}}
     {\text{offer}(\{c_1, \ldots, c_n\})}
\end{equation}

\textbf{R2. Batch verification} (W1-queue $\Rightarrow$ verdict-set):
\begin{equation}
\frac{\text{queue} = \{c_1, \ldots, c_n\},
      \forall i.\ \text{W1}(c_i, p) = \langle c_i, s_i, q_i \rangle}
     {\text{verdicts} = \{\langle c_1, s_1, q_1 \rangle, \ldots,
      \langle c_n, s_n, q_n \rangle\}}
\end{equation}

\textbf{R3. Provenance commit} (verdict $\Rightarrow$ depends-on edge):
\begin{equation}
\frac{\text{promote}(\langle c, s, q \rangle) = v_{\text{claim}},
      \text{source}(q) = v_{\text{evidence}}}
     {E' = E \cup \{(v_{\text{evidence}}, v_{\text{claim}})\},
      \lambda(v_{\text{evidence}}, v_{\text{claim}}) = \mathtt{depends\_on}}
\end{equation}

\textbf{R4. Stream mode} (online term rewriting):
\begin{equation}
\mathcal{G}(t) \xrightarrow{\text{W1}_{\text{stream}}} \mathcal{G}(t+\Delta t)
\quad \text{where } \Delta t < \text{read-completion-time}
\end{equation}
Verification proceeds concurrently with document reading, not sequentially after.

\textbf{Seal condition:}
\begin{equation}
\forall v \in V.\ \sigma(v) = \mathtt{candidate} \implies
  \exists \text{review-action}(v) \lor
  \text{age}(v) < \epsilon
\end{equation}
No unreviewed candidate older than $\epsilon$ (stale threshold).

\end{document}

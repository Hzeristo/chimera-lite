# ARA Structure Audit — arXiv 2604.24658 (Phase Q re-grounding)

**Paper:** "The Last Human-Written Paper: Agent-Native Research Artifacts" (Liu, Pei, Si, … Pentland,
Ang Chen, Chowdhury, Zhang, et al.), arXiv **2604.24658**.
**ARA =** **Agent-Native Research Artifact** — *not* "Agentic Reproducibility Assessment" (that is
**2605.02651**, the paper wrongly cited in the Phase Q spec).
**Why this audit:** Phase Q was grounded on the wrong paper. The Architect identified the correct one;
this audit extracts its real structure so Phase Q can be re-grounded. Read-only; the one write is this file.
**Date:** 2026-07-09
**Sources:** `https://arxiv.org/abs/2604.24658` · `https://arxiv.org/html/2604.24658v1` (§1, §2.1, §2.2, §3–§5 as fetched).

---

## ⚠️ Headline correction (feeds Step-2 B2 + Mission)

The Phase Q spec (Mission + Decision 4) describes the ARA structure as a **"Sources → Methods →
Experiments → Outputs" workflow graph.** **That is the WRONG paper's structure** — 2605.02651
(Agentic Reproducibility Assessment) literally builds a directed `sources→methods→experiments→outputs`
workflow graph. The CORRECT ARA (2604.24658) does **not** use that schema anywhere (confirmed §2, §2.1).
Its structure is a **four-layer artifact**. Every Phase Q reference to "sources→methods→experiments→outputs"
must be replaced with the four layers below.

---

## Q1 — The two distinct parts (confirm / correct)

The Architect's belief — (1) a method for abstracting papers into structured graphs, (2) a framework for
how the structure guides auto-research — is **CONFIRMED**, refined as two load-bearing parts + supporting
infra + validation:

1. **The ARA artifact format (the abstraction)** — §2: a paper is re-cast from linear narrative into an
   **agent-executable knowledge package** of four layers (Q2). This is the "structure" part.
2. **The operational framework (auto-research support)** — the ecosystem that produces and consumes ARA
   artifacts:
   - **Live Research Manager** (§3) — captures decisions and dead-ends as side-effects of everyday
     development (builds `/trace` continuously).
   - **ARA Compiler** (§4) — converts legacy PDFs/repos into ARA format. **This is the piece Chimera's
     ingest emulates.**
   - **ARA-Native Review System** (§5) — automated structural verification + budget-aware reproduction.
   - Plus empirical validation (improved agent QA accuracy + reproduction success).

Motivation (§1): two "taxes" ARA removes — the **Storytelling Tax** (linear narrative discards failed
experiments / rejected hypotheses / branching exploration) and the **Engineering Tax** (prose sufficient
for reviewers ≠ specification sufficient for agents).

---

## Q2 — Internal structure (nodes, edges, primitives)

**Four layers** (§2.1 "A structured knowledge vessel"), each a top-level directory:

| Layer | Dir | Agent question | Atomic primitives |
|---|---|---|---|
| Cognitive | `/logic` | *why* does it work | `problem.md`, `solution/`, **`claims.md`** (each claim: Statement · Status · Falsification criteria · Proof), **`experiments.md`** (Metrics · claim linkage), `related_work.md`, heuristics (Trick · sensitivity · bounds) |
| Physical | `/src` | *how* is it built | code — kernel mode (core modules w/ typed I/O signatures) or repository mode (full impl + `index.md`); `configs/` (rationale · search range); `environment.md` |
| Exploration | `/trace` | *what was tried* | `exploration_tree.yaml` — a nested YAML **DAG**; **five typed node kinds**: `question`, `decision`, `experiment`, `dead_end`, `pivot`. Dead-end leaves preserve hypothesis · failure mode · lesson |
| Evidence | `/evidence` | *what are the numbers* | `results/` (machine-readable metric tables, generated data), `logs/` (training curves, diagnostics) |

**Edge types:**
- **Cross-layer "forensic bindings" (§2.2)** — the proof chain **`claims.md → experiments.md → /evidence/`**,
  plus claims→`/src` code references and experiment-ID resolution. **INTRA-artifact** provenance (binds one
  paper's own claims to its own evidence/code).
- **`/trace` edges** — **nesting = parent→child**; **`also_depends_on` = convergence** (a DAG, not a tree).
- **Cross-artifact** — via `related_work.md` / imports ("imports inject prior definitions"; "baseline
  entries enable automatic regression detection"). The **only** edges that reach outside the single artifact.

---

## Q3 — Mapping ARA structure → vault K/T/I/D (interpretive; the Architect's call)

**Two graphs, do not conflate** (this is Phase Q's B2 point, now grounded in the real structure):
- **ARA intra-artifact structure** = how ONE paper is assembled (claims↔experiments↔evidence, trace DAG)
  → maps to the **internal content of a single vault node (or a per-paper node cluster)**, NOT to typed edges.
- **Vault typed edges** (`derives_from` / `synthesizes` / `contradicts`) = how THIS paper relates to OTHER
  vault nodes → sourced from ARA's **cross-artifact** references (`related_work.md`/imports), surfaced by the
  **Grounding** step.

Proposed primitive → node mapping (my interpretation):

| ARA primitive | Vault node | Rationale |
|---|---|---|
| `/logic` claims + `/evidence` | **K** (Knowledge) | what the paper asserts and proves; the claims→experiments→evidence binding becomes the K node's internal proof structure |
| `/logic` **heuristics** (Trick · sensitivity · bounds) | **I** (Insight) | the transplantable mechanism — cross-domain-thief gold |
| `/trace` **`decision`** nodes | **D** (Decision) | direct correspondence |
| `/trace` **`dead_end` / `pivot` / `question`** + reader's interpretation | **T** (Thought) | process-knowledge & the reader's own reading; "what didn't work and why" is high-value |

Edge mapping:

| Vault edge | Source | Note |
|---|---|---|
| **`derives_from`** (→ prior vault node) | **Grounding step** (found a prior node) + ARA `related_work.md`/imports | **NOT** from intra-artifact extraction. Only when a real prior node is found. |
| **`contradicts`** | Grounding + claim comparison (this paper's claim vs a prior node's claim) | |
| **`synthesizes`** | a Thought/Insight combining ≥2 prior nodes | |

**Consequence for Phase Q:** a `derives_from` edge to a prior vault node comes from **Grounding**, not from
ARA extraction — this validates the *intent* of the Architect's B2 patch, but the patch's primitive names
("method node → T/I", "experiment node → T") are wrong-paper residue and should be replaced with the tables
above.

---

## Q4 — Function-based, not type-based (CRITICAL for lens triggering)

**Unambiguously FUNCTION-based** (§2.1 "A structured knowledge vessel"). The four layers exist because an
agent asks four **structurally distinct questions / has four needs**:
> *why* it works (reasoning) · *how* it's implemented (code) · *what was tried* (trajectory) · *what are the
> numbers* (evidence) — needs: *understand · reproduce · learn-from-process · verify* (§2.2).

The paper explicitly does **not** categorize by content type ("this is a math paper"). It categorizes by
**role / function** ("this content serves to justify *why*" / "this is *evidence* for a claim").

**Implication for Phase Q lens demotion (G1 — CONFIRMED):** lenses trigger on content **function**, not type
— not "paper has math → Math Lens" but "a `claim` in `/logic` has `Status: asserted` and its `experiments.md`
linkage resolves to no `/evidence/` entry → `result_ungrounded` → Forensic-Leakage Lens." The extraction
schema's flag vocabulary should be **functional defects** (`claim_without_evidence`, `experiment_orphaned`,
`config_without_rationale`, `dead_end_unrecorded`, `math_decoration`), not paper-type labels.

---

## Bottom line for Phase Q re-grounding
1. Replace every "Sources→Methods→Experiments→Outputs" with the **four layers** (`/logic` `/src` `/trace` `/evidence`).
2. Chimera's *read* extraction target = `/logic` (claims + heuristics + experiments) + `/evidence` + salient
   `/trace` (dead_ends/pivots). We are a **reader/compiler** (the §4 Compiler analogue), not re-running the
   research, so `/src` and raw `/evidence` logs are reference, not vault content.
3. G1 (function-based lens triggering) is **confirmed** (§2.1/§2.2).
4. B2's *intent* holds (intra vs inter graph; `derives_from` from grounding) but its *primitive names* are
   wrong-paper residue — use the Q3 tables.

*Read-only audit. No spec edited — Step 2 pending Architect confirmation of the corrected structure (below).*

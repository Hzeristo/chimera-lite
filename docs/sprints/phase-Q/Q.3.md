# Modification Summary: Q.3

**Phase:** Q
**Sprint:** Q.3 — grounding across the 3 real vault K-node schemas
**Batch position:** 5 of 6
**Date:** 2026-07-09
**Executed by:** Sonnet subagent (chimera-code-taste batch delegation); reviewed + committed by main session.

---

## Files touched

| Path | +Added | Notes |
|---|---|---|
| `tests/test_extract_schemas.py` | +~90 (new) | 4 tests — Schema A/B/C fixtures + duplicate-migration, all via `resolve_citations` |

---

## Verification

| Check | Status | Output |
|---|---|---|
| ruff | clean | exit 0 |
| pytest (`test_extract_schemas.py`) | 4/4 | exit 0 |

---

## Acceptance Criteria

| Criterion (batch plan Q.3) | Status | Evidence |
|---|---|---|
| Schema A resolves (string `score`, no `arxiv_id`) | Met | `test_schema_a_resolves_despite_string_score_and_no_arxiv_id` |
| Schema B & C resolve by stem | Met | `test_schema_b_resolves`, `test_schema_c_resolves` |
| Duplicate-migration → exactly one proposal | Met | `test_duplicate_migration_pair_resolves_to_exactly_one_node` |

---

## Notes

- Confirms the design fact from the re-audit: grounding/supersede match a K node by **stem + `type: knowledge`**,
  independent of the rest of the frontmatter — so extraction/backfill works uniformly across the 3 schema
  generations. (The mechanism-vs-recipe *claim* discipline remains prompt-enforced + validated on real LLM
  output in Q.4, not structurally.)
- Test-only; no production code touched.

---

## Commit Status

- [x] Committed

```
test(tests): Q.3 — grounding across the 3 vault K-node schemas (A/B/C + duplicate)
```

---

**Sprint result: Pass.** Next: Q.4 (🟡 live backfill of the 13 — preconditions being verified).

---

*Generated by chimera-code-taste batch_execution mode, per-sprint summary.*

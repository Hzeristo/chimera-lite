# Modification Summary: Q.1b

**Phase:** Q
**Sprint:** Q.1b — `create_staging_node` metadata passthrough (D4)
**Batch position:** 2 of 6
**Date:** 2026-07-09
**Executed by:** Sonnet subagent (chimera-code-taste batch delegation); reviewed + committed by main session.

---

## Files touched

| Path | +Added | -Removed | Notes |
|---|---|---|---|
| `mcp-servers/chimera-papers/staging_service.py` | +9 | -0 | `create_staging_node` gains `metadata: dict | None = None`; `if metadata: fm.update(metadata)`; docstring |
| `tests/test_staging_metadata.py` | +~30 | -0 | New — 2 tests (metadata merged; `metadata=None` byte-compat) |

---

## Verification

| Check | Status | Output Summary |
|---|---|---|
| ruff | clean | exit 0 — "All checks passed!" |
| pytest (`test_staging_metadata.py`) | 2/2 | exit 0 |
| pytest (regression: `test_staging_tools.py` + new) | 12/12 | exit 0 — no regression to existing staging tests |
| check_taste.ps1 | N/A | crucible_core-targeted (M.tooling.1) |

---

## Rule Conformance Self-Check

| Rule | Status | Evidence |
|---|---|---|
| DDD layering preserved | ✓ | change confined to `StagingService` (service layer) |
| pydantic_defaults | N/A | no schema change |
| exception_handling | N/A | none added |
| no_opportunistic_refactor | ✓ | only `create_staging_node` touched; other methods untouched |
| backward compatibility | ✓ | `metadata=None` path is a no-op guard; key set test asserts it |

---

## Red Line Status

| Red Line | Status | Verification |
|---|---|---|
| `metadata=None` byte-identical to today | Held | backward-compat test asserts frontmatter key set = `{type,status,title,created_at,tags,graph_edges}` |
| Only `create_staging_node` changes | Held | `stage_link_patch` / `promote_node` / `apply_link_patch` / `_splice_graph_edges` untouched |
| Staging-only, no live-vault write | Held | writes to `staging_dir` only |
| 不进行机会主义重构 | Held | two files only |

---

## Acceptance Criteria

| Criterion (batch plan Q.1b) | Status | Evidence |
|---|---|---|
| `metadata={provenance,grounded}` emits those keys | Met | test 1 |
| `metadata=None` unchanged from today | Met | test 2 (exact key-set assertion) |

---

## Notes

- Enables HSC 2 (`grounded: no_prior_match`) + HSC 3 (provenance tags) for Q.2b/Q.4 — the K node's extra frontmatter now has a home.
- `yaml.dump` sorts keys, so metadata ordering is automatic and the `metadata=None` output is unchanged.

---

## Commit Status

- [x] Committed (bundled with Q.2a note or its own commit)

```
feat(crucible): Q.1b — create_staging_node metadata passthrough
```

---

**Sprint result: Pass.** Proceeding to Q.2a (parallel), then the gated Q.2b.

---

*Generated by chimera-code-taste batch_execution mode, per-sprint summary.*

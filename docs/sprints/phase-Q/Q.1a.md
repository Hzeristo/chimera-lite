# Modification Summary: Q.1a

**Phase:** Q
**Sprint:** Q.1a — `KClaimExtraction` response_model
**Batch position:** 1 of 6
**Date:** 2026-07-09
**Executed by:** Sonnet subagent (chimera-code-taste batch delegation); reviewed + committed by main session.

---

## Files touched

| Path | +Added | -Removed | Notes |
|---|---|---|---|
| `mcp-servers/chimera-papers/core/schemas.py` | +37 | -0 | New `# --- Phase Q: Knowledge extraction ---` section: `ClaimFlag`, `ExtractedClaim`, `EdgeProposal`, `KClaimExtraction` |
| `tests/test_kclaim_schema.py` | +117 | -0 | New — 12 structural-contract tests |

---

## Verification

| Check | Status | Output Summary |
|---|---|---|
| ruff | clean | exit 0 — "All checks passed!" |
| mypy | not run | chimera-lite gate = ruff + pytest (Accepted Partial M.tooling.1) |
| pytest (`test_kclaim_schema.py`) | 12/12 | exit 0 — `12 passed in 0.07s` |
| check_taste.ps1 | N/A | targets `crucible_core/`, not chimera-lite (M.tooling.1) |

---

## Rule Conformance Self-Check

| Rule | Status | Evidence |
|---|---|---|
| DDD layering preserved | ✓ | models live in `core/` (the data dictionary); no cross-layer import |
| function_naming | N/A | no functions added (test helpers `_claim`/`_edge` local) |
| abstraction_threshold (rule of 3) | N/A | no abstraction introduced |
| exception_handling | N/A | none added |
| pydantic_defaults (extra="forbid", Field descriptions) | ✓ | all three models `ConfigDict(extra="forbid")`; every field has a `description` |
| ui_tokens | N/A | no UI |
| logging_format | N/A | no logs |
| no_opportunistic_refactor | ✓ | only the two files touched; nothing else in `schemas.py` changed |

---

## Red Line Status

| Red Line | Status | Verification |
|---|---|---|
| No numbers in `statement` | Held (schema-level) | `statement` `Field` description forbids it; **semantic enforcement deferred to Q.2b prompt + Q.3 test** (schema cannot enforce mechanism-vs-recipe) |
| No new dependency; model in `schemas.py` only | Held | no new imports (reused `Enum`/`Literal`/`BaseModel`/`ConfigDict`/`Field`); only `schemas.py` |
| No I/T/D fields | Held | `test_no_model_admits_an_itd_field` passes (scans `model_fields` for insight/thought/decision) |
| 不进行机会主义重构 | Held | only `schemas.py` + the new test file changed |

---

## Acceptance Criteria

| Criterion (batch plan Q.1a) | Status | Evidence |
|---|---|---|
| `model_json_schema()` renders; `extra="forbid"` rejects unknown keys | Met | `test_model_json_schema_renders`, `test_*_rejects_unknown_key` |
| Name-deletion check in the test | **Re-scoped** | the mechanism-vs-recipe / name-deletion check is *semantic* and cannot live in a pure Pydantic schema — moved to the Q.2b extraction prompt + the Q.3 discipline test. Q.1a asserts the structural contract instead. Flagged for phase_review. |
| No field admits an I/T/D node | Met | `test_no_model_admits_an_itd_field` |

---

## Notes

- **Re-scope (not an Accepted Partial yet):** the batch plan's Q.1a acceptance named a "name-deletion lint" in the test. That is a *semantic* check (is a statement a mechanism or a recipe?) which a Pydantic schema structurally cannot perform, and building a linter into `schemas.py` would violate its "pure data dictionary" red line. The discipline is enforced where it belongs — the Q.2b extraction prompt (instructs mechanism statements) and the Q.3 discipline test (asserts on real LLM output). Surface at phase_review.
- Models placed after `DeepReadAtlas`, before `SkillDefinition`.
- **Handoff:** Q.2b will `generate_structured_data(response_model=KClaimExtraction)`; Q.2a's `EdgeProposal` shape is consumed here.

---

## Commit Status

- [x] Files staged
- [x] Commit drafted (Tier-2)
- [x] Commit applied

```
feat(crucible): Q.1a — KClaimExtraction response_model (Phase Q extraction)
```

---

**Sprint result: Pass.** Proceeding to Q.1b + Q.2a (parallel-eligible 🟡 prep).

---

*Generated by chimera-code-taste batch_execution mode, per-sprint summary.*

# Modification Summary: Q.2a

**Phase:** Q
**Sprint:** Q.2a — citation-resolution grounding (D3)
**Batch position:** 3 of 6
**Date:** 2026-07-09
**Executed by:** Sonnet subagent (chimera-code-taste batch delegation); reviewed + committed by main session.

---

## Files touched

| Path | +Added | -Removed | Notes |
|---|---|---|---|
| `mcp-servers/chimera-papers/grounding.py` | +113 | -0 | New — `resolve_citations(references, vault_root) -> list[EdgeProposal]` + helpers |
| `tests/test_grounding.py` | +109 | -0 | New — 7 tests (id-match, no-match, `.migration_backup` excluded, thought-node excluded) |

---

## Verification

| Check | Status | Output Summary |
|---|---|---|
| ruff | clean | exit 0 — "All checks passed!" |
| pytest (`test_grounding.py`) | 7/7 | exit 0 — `7 passed in 0.11s` |

---

## Rule Conformance Self-Check

| Rule | Status | Evidence |
|---|---|---|
| DDD layering preserved | ✓ | `grounding.py` sits in the papers domain; imports only `core.schemas` (core layer) — one direction |
| exception_handling | ✓ | frontmatter read wrapped in narrow `except (UnicodeDecodeError, OSError, LookupError)` + defensive YAML skip; no bare `BaseException` swallowing control flow |
| logging_format | ✓ | `[Vault]`-prefixed warning on malformed frontmatter |
| no_opportunistic_refactor | ✓ | two new files; `vault_read_adapter.py` NOT modified (its exclusion gap is fixed *for grounding* by this module's own `_NONVAULT_DIRS`) |

---

## Red Line Status

| Red Line | Status | Verification |
|---|---|---|
| Citation-resolution only — no similarity edges | Held | `resolve_citations` matches a full arxiv id (`\d{4}\.\d{4,5}`) contained in a stem, or an EXACT stem — never keyword/topic. Reviewed `grounding.py:91-96` |
| Exclude `.obsidian` / `.migration_backup` / `templates` | Held | `_NONVAULT_DIRS & set(rel.parts)` skip (`grounding.py:60`); dedicated test asserts a `.migration_backup` K node is never proposed |
| Read-only over the vault | Held | no write calls; only `read_text` / `rglob` |
| No new dependency; no opportunistic refactor | Held | stdlib + `yaml` + `pydantic` only; two new files |

---

## Acceptance Criteria

| Criterion (batch plan Q.2a) | Status | Evidence |
|---|---|---|
| arxiv id present in vault → `derives_from` proposal to that stem | Met | test: `["2305.16291"]` → proposal to `2305.16291-VOYAGER` |
| id not in vault → no proposal | Met | test |
| never proposes a `.migration_backup` / `templates` file | Met | dedicated exclusion test |

---

## Notes

- **`contradicts` deferred (scope):** `resolve_citations` emits only `derives_from`. `contradicts` needs semantic claim-conflict detection (compare this paper's claim vs a prior node's), not citation-resolution — it belongs to Q.2b's extraction (via `KClaimExtraction.proposed_edges`) or a later refinement, not the grounding sub-routine. Flagged for phase_review.
- Duplicate-migration papers (e.g. `2409.07429-AWM` Schema-C vs `2409.07429v1-AWM` Schema-A) both contain the same id; `next(...)` picks the first by rglob order — same paper either way, not a fabrication. Minor, non-blocking.
- **Handoff:** Q.2b calls `resolve_citations(paper_references, vault_root)`; an empty return → the caller stages `grounded: no_prior_match` (edgeless).

---

## Commit Status

- [x] Committed

```
feat(papers): Q.2a — citation-resolution grounding (derives_from proposals)
```

---

**Sprint result: Pass.** Prep complete (Q.1a/Q.1b/Q.2a). **Next: Q.2b 🔴 — GATED, awaiting explicit user approval.**

---

*Generated by chimera-code-taste batch_execution mode, per-sprint summary.*
